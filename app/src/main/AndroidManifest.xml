<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:label="@string/activityPMuitoJusto" android:name=".view.MuitoJusto" />
        <activity android:label="@string/activityAutonomia" android:name=".view.Autonomia" />
        <activity android:label="@string/activityCalculadora" android:name=".view.Calculadora" />
        <activity android:label="@string/activityPJ" android:name=".view.PrecoJusto" />
        <activity android:label="@string/activityCalcularMedia" android:name=".view.CalcularMedia" />

        <activity android:name=".view.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>